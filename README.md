
Installing
==========

    $ cd ~/.vim
    $ git submodue init
    $ git submodule update
    $ ./setup.sh

Generating tags
===============
    - First edit "generate_tags" to your liking.

    $ cd ~/.vim
    $ ./generate_tags

